<style>
  /* .btn-search{
    background-color: #ed3d6f;
    color: #fff;
    border-radius: 25px;
    padding: 0 1rem 0 1rem;
  }

  .btn-search:hover{
    background-color: #ed3d6f;
    color: #fff;
  }

  .btn-search i{
    display: inline;
    margin-right: 0.5rem;
  }

  .inp{
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size: 1rem;
  } */
  .btn-search {
    background-color: #ed3d6f;
    color: #fff;
    border-radius: 25px;
    padding: 0 1rem 0 1rem;
  }

  .btn-search:hover {
    background-color: #ed3d6f;
    color: #fff;
  }

  .btn-search i {
    display: inline;
    margin-right: 0.5rem;
  }

  .inp {
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size: 1rem;
  }

  /* Media query for mobile screens */
  @media (max-width: 768px) {
    .inp {
      font-size: 0.875rem; /* Smaller font size for mobile */
      padding: 0.5rem 2rem 0.5rem 2rem; /* Adjust padding for mobile */
    }

    .navbar-nav {
      margin-top: 0.5rem;
    }

    .navbar-nav.ms-auto {
      margin-left: 0;
    }

    .navbar-collapse {
      text-align: start;
    }

    .btn-search {
      padding: 0.5rem 1rem; /* Adjust button padding */
      font-size: 0.875rem; /* Smaller button text for mobile */
    }

    .nav-link {
      font-size: 0.875rem; /* Smaller nav link text */
      padding: 0.5rem 0;
    }

    .navbar-nav .nav-link {
      text-align: start;
    }
  }

  /* Prevent layout shift on navbar toggle */
  .navbar {
    position: relative; /* Prevent layout shifting */
    z-index: 10; /* Ensure navbar stays on top */
  }

  /* Prevent page content below navbar from shifting when toggling */
  .navbar-collapse {
    padding-top: 0; /* Remove any extra top padding on collapse */
  }

  .navbar-toggler {
    z-index: 20; /* Make sure the toggler button stays above other content */
  }

</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
       
          <a class="nav-link" href="/listings">Explore</a>
         
          
        </div>

       
        <div  class="navbar-nav ms-auto">
          <form class="d-flex" role="search">
            <input class="form-control me-2 inp" type="search" placeholder="Search Destination">
            <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
          </form>
        </div>

        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if(!curUser) {  %>  
          <a class="nav-link" href="/signup" style="color: #ed3d6f !important;"><b>Sign Up</b></a>
            <a class="nav-link" href="/login" style="color: #ed3d6f !important;"><b>Log In</b></a>
          <% } %>
        
        <%  if(curUser) { %> 
          
            <a class="nav-link" href="/logout"><b>Log Out</b></a>
        <% } %>  
         
          
        </div>
      </div>
    </div>
  </nav>